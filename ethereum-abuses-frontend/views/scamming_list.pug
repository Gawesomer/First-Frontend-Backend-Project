extends layout

block content
  h1= title

  ul
    each scamming in scamming_list
      li
        a(href=scamming.url) #{scamming.name}
        |  (#{scamming.category})

    else
      li There are no scams.
