extends layout

block content
  h1= title

  ul
    each phishing in phishing_list
      li
        a(href=phishing.url) #{phishing.name}
        |  (#{phishing.category})

    else
      li There are no phishing abuses.
